// convex/schema/system/supporting/availability/validators.ts
// Grouped validators for availability module

import { v } from 'convex/values';

// Availability has no union validators currently
// But we include this file for consistency
export const availabilityValidators = {} as const;

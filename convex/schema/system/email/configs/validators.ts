// convex/schema/system/email/configs/validators.ts
// Validators for email configs module (wraps shared email validators/fields)

import { emailValidators, emailFields } from '../validators';

export const emailConfigsValidators = emailValidators;
export const emailConfigsFields = emailFields;
